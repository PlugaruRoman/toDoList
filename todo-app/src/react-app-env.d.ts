/// <reference types="react-scripts" />

type Todos = {
  id: number;
  name: string;
  priority: string;
  checked: boolean;
};

type Todo = {
  name: string;
  priority: string;
};

type Category = {
  title: string;
};
